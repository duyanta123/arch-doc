[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "demo-api"
version = "0.1.0"
description = "Demo API service"
dependencies = [
  "fastapi>=0.115.0",
  "uvicorn",
]
